% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eval.R
\name{evaluate}
\alias{evaluate}
\title{Evaluate a target over a dataset}
\usage{
evaluate(target, dataset, evaluators = list(), ...)
}
\arguments{
\item{target}{An agent object or a function(input, ...) returning an answer}

\item{dataset}{An \code{\link[=eval_dataset]{eval_dataset()}}, a data.frame with an \code{input} column
(and optional \code{expected}), or a character vector of inputs}

\item{evaluators}{A list of evaluator objects (from \code{\link[=eval_exact_match]{eval_exact_match()}},
\code{\link[=eval_llm_judge]{eval_llm_judge()}}, \code{\link[=eval_custom]{eval_custom()}}, ...)}

\item{...}{Extra arguments forwarded to the target on every example}
}
\value{
An evaluation result: a list with \code{results} (per-example data
frame), \code{summary} (per-evaluator aggregates), \code{details} (per-example
evaluator reasons), and \code{passed} (overall)
}
\description{
Runs \code{target} on every dataset example and scores each answer with every
evaluator. \code{target} is either an agent (built with \code{\link[=chat_agent]{chat_agent()}},
\code{\link[=react_agent]{react_agent()}}, etc.) or a function of the input that returns the answer
(a string, or a list with an \code{answer} element). Extra arguments in \code{...}
are forwarded to \code{\link[=run_agent]{run_agent()}} or the function on every call.
}
\details{
Errors in the target or in an evaluator are contained per example: the run
continues and the failure is recorded.
}
