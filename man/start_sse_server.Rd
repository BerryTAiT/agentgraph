% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sse.R
\name{start_sse_server}
\alias{start_sse_server}
\title{Serve an agent's tokens over Server-Sent Events}
\usage{
start_sse_server(
  target,
  host = "127.0.0.1",
  port = 0,
  auth_token = NULL,
  name = "agent",
  timeout = 30
)
}
\arguments{
\item{target}{An agent (from \code{\link[=chat_agent]{chat_agent()}}, etc.) or a graph (from \code{\link[=state_graph]{state_graph()}})}

\item{host}{Interface to bind (default "127.0.0.1")}

\item{port}{Port to bind (0 = random free port)}

\item{auth_token}{Optional bearer token required for /stream}

\item{name}{Optional name for the handle}

\item{timeout}{Seconds to wait for readiness}
}
\value{
A server handle (class \code{agentgraph_sse_server})
}
\description{
Starts a background server that streams the agent's tokens as
\code{text/event-stream}. \code{POST /stream} (JSON body \code{{"input":"..."}}) and
\code{GET /stream?input=...} each emit one \verb{data: <token>} event per token and a
final \verb{data: [DONE]}. \code{GET /health} is open; when \code{auth_token} is set,
\verb{/stream} requires an \verb{Authorization: Bearer <token>} header. Stop it with
\code{\link[=sse_stop]{sse_stop()}}.
}
