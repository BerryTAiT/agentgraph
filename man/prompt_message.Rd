% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prompts.R
\name{prompt_message}
\alias{prompt_message}
\title{Render a prompt template into a message object}
\usage{
prompt_message(prompt, ..., vars = list())
}
\arguments{
\item{prompt}{A \code{\link[=prompt_template]{prompt_template()}} or a single character string}

\item{...}{Named variables}

\item{vars}{Named list of variables}
}
\value{
A message list: \code{list(role, content)}
}
\description{
Renders the template (same rules as \code{\link[=render_prompt]{render_prompt()}}) and wraps the result
in a message list using the template's \code{role} (default "system"; raw
strings render as "user"), so it can be passed directly as a message.
}
