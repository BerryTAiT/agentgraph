% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{serve_agent}
\alias{serve_agent}
\title{Serve an agent or graph over a REST API}
\usage{
serve_agent(
  target,
  host = "127.0.0.1",
  auth_token = NULL,
  name = "agent",
  timeout = 30
)
}
\arguments{
\item{target}{An agent (from \code{\link[=chat_agent]{chat_agent()}}, \code{\link[=react_agent]{react_agent()}}, ...) or a graph
(from \code{\link[=state_graph]{state_graph()}})}

\item{host}{Interface to bind (default "127.0.0.1")}

\item{auth_token}{Optional bearer token required for /run and /stream}

\item{name}{Optional name reported in the handle}

\item{timeout}{Seconds to wait for the server to report readiness}
}
\value{
A server handle (class \code{agentgraph_serve})
}
\description{
Starts a background server (a separate R process) exposing \code{target} via a
minimal JSON REST API: \code{GET /health}, \code{POST /run} (returns \code{answer} +
\code{state}), and \code{POST /stream} (returns \code{answer} + the token sequence captured
via \code{on_token}). Requests send a JSON body \code{{"input": "..."}}. When
\code{auth_token} is set, \verb{/run} and \verb{/stream} require an
\verb{Authorization: Bearer <token>} header (health stays open). Stop the server
with \code{\link[=serve_stop]{serve_stop()}}.
}
\details{
Only graph-based agents and graphs can be served (they serialize to the
server process); a \code{router_agent()} carries an R closure and is rejected.
}
