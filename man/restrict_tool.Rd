% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy.R
\name{restrict_tool}
\alias{restrict_tool}
\title{Wrap a tool so its calls are validated against a policy}
\usage{
restrict_tool(tool, policy)
}
\arguments{
\item{tool}{A tool definition from \code{\link[=tool]{tool()}}}

\item{policy}{A \code{\link[=tool_policy]{tool_policy()}}}
}
\value{
A new tool definition with a validating handler
}
\description{
Returns a copy of \code{tool} whose handler first validates the incoming
arguments with \code{\link[=validate_tool_args]{validate_tool_args()}} and only then invokes the original
handler. A violating call raises an error, which the engine surfaces to the
LLM as a failed tool result. The wrapper is self-contained (base R only), so
it serializes into the tool-server process unchanged. Composable with
\code{\link[=guarded_tool]{guarded_tool()}} (apply validation and output sanitization together).
}
