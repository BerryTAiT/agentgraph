% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{provider_bedrock}
\alias{provider_bedrock}
\title{Create an AWS Bedrock provider configuration}
\usage{
provider_bedrock(
  aws_access_key_id = Sys.getenv("AWS_ACCESS_KEY_ID"),
  aws_secret_access_key = Sys.getenv("AWS_SECRET_ACCESS_KEY"),
  aws_session_token = Sys.getenv("AWS_SESSION_TOKEN"),
  region = Sys.getenv("AWS_REGION", "us-east-1"),
  model = "us.anthropic.claude-sonnet-4-20250514-v1:0",
  max_tokens = 4096,
  temperature = 0.7,
  max_retries = 3L,
  retry_base_delay_ms = 500L,
  retry_max_delay_ms = 8000L,
  requests_per_minute = 0L
)
}
\arguments{
\item{aws_access_key_id}{AWS access key (or set AWS_ACCESS_KEY_ID env var)}

\item{aws_secret_access_key}{AWS secret key (or set AWS_SECRET_ACCESS_KEY env var)}

\item{aws_session_token}{Optional session token for temporary credentials
(or set AWS_SESSION_TOKEN env var)}

\item{region}{AWS region (or set AWS_REGION env var)}

\item{model}{Bedrock model ID, e.g. "us.anthropic.claude-sonnet-4-20250514-v1:0"}

\item{max_tokens}{Maximum tokens in response}

\item{temperature}{Sampling temperature}

\item{max_retries}{Retry transient HTTP failures (429/5xx); 0 disables}

\item{retry_base_delay_ms}{Initial backoff delay (ms), doubles each attempt}

\item{retry_max_delay_ms}{Upper bound on the backoff delay (ms)}

\item{requests_per_minute}{Token-bucket request cap; 0 disables rate limiting}
}
\value{
A provider configuration list
}
\description{
Authenticates with AWS Signature Version 4 (implemented in the C++ engine)
against Bedrock's OpenAI-compatible endpoint. Credentials default to the
standard AWS environment variables.
}
