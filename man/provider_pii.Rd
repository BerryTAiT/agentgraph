% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{provider_pii}
\alias{provider_pii}
\title{Enable PII scrubbing on a provider configuration}
\usage{
provider_pii(provider, redact = "[REDACTED]")
}
\arguments{
\item{provider}{A provider configuration list (from \code{provider_openai()},
\code{provider_anthropic()}, \code{provider_fallback()}, etc.)}

\item{redact}{Replacement text for redacted values}
}
\value{
A provider configuration list with PII filtering enabled
}
\description{
Wraps an existing provider (or fallback chain) so that message content,
text parts, tool results, tool-call arguments, and the system prompt are
redacted for common PII — email addresses, API keys, US SSNs, phone
numbers, credit-card-like digit runs, and IPv4 addresses — before every
outbound HTTP call to the LLM endpoint. Scrub patterns match the
standalone \code{\link[=pii_scrub]{pii_scrub()}} helper.
}
\examples{
provider_pii(provider_openai(model = "gpt-4o"))
}
