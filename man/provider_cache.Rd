% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{provider_cache}
\alias{provider_cache}
\title{Enable exact LLM caching on a provider configuration}
\usage{
provider_cache(provider, ttl_seconds = 300, max_entries = 1000)
}
\arguments{
\item{provider}{A provider configuration list (from \code{provider_openai()},
\code{provider_anthropic()}, \code{provider_fallback()}, etc.)}

\item{ttl_seconds}{Cache entry lifetime in seconds; \code{0} means never expire}

\item{max_entries}{Maximum cached entries for this provider; \code{0} means unlimited}
}
\value{
A provider configuration list with caching enabled
}
\description{
Wraps an existing provider (or fallback chain) so that identical requests are
served from an in-process cache instead of hitting the HTTP API. The cache key
is the full request (messages + tools + system prompt) plus the
provider/model/sampling identity, so two calls are only considered identical
when they would produce the same response. Entries persist across \code{chat()},
\code{stream()}, and graph runs within the same R process.
}
\examples{
provider_cache(provider_openai(model = "gpt-4o"), ttl_seconds = 60)
}
