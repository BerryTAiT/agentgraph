% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{batch_submit}
\alias{batch_submit}
\title{Submit a batch as an async background job}
\usage{
batch_submit(target, inputs, ..., concurrency = 1L)
}
\arguments{
\item{target}{An agent, a graph, or a function}

\item{inputs}{A character vector or list of inputs}

\item{...}{Extra arguments forwarded to the target}

\item{concurrency}{Number of parallel workers (1 = sequential in the job)}
}
\value{
A batch job (class \code{agentgraph_batch_job})
}
\description{
Launches \code{\link[=batch]{batch()}} in a background mirai process and returns immediately
with a job handle. Poll it with \code{\link[=batch_status]{batch_status()}} and retrieve the results
with \code{\link[=batch_collect]{batch_collect()}}. Arguments are identical to \code{\link[=batch]{batch()}} and are
validated eagerly, so an invalid job fails at submission time. Requires
the mirai package (even for \code{concurrency = 1} — the background evaluation
itself runs in a daemon).
}
