# Launcher for the Streamly subscription agent console.
#
# Lets you start the agent from the repo root regardless of where the R
# console's working directory is:
#
#   source("start_subscription_agent.R")
#
# (source("main.R") only works when the working directory is already
# subscription-agent/, because source() resolves the file before main.R's
# self-locating code can run.)
#
source("c:/Users/berry/Desktop/langgraphc++/subscription-agent/main.R")
