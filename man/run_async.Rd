% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/async.R
\name{run_async}
\alias{run_async}
\title{Run a target as an async background job}
\usage{
run_async(target, input, ...)
}
\arguments{
\item{target}{An agent, a graph, or a function}

\item{input}{The input for the target}

\item{...}{Extra arguments forwarded to the target}
}
\value{
A job handle (class \code{agentgraph_job}) with a \verb{$id}
}
\description{
Launches \code{target} in a background mirai process and returns immediately with
a job ID. Poll it with \code{\link[=job_status]{job_status()}}, retrieve the result with
\code{\link[=job_result]{job_result()}}, or cancel with \code{\link[=job_cancel]{job_cancel()}}. Requires the mirai package.
}
\details{
\code{target} may be an agent (each \code{input} a single string), a graph (each
\code{input} a single string or a state list), or a function (\code{target(input, ...)}).
For agent/graph targets the full result (answer + state, or the state) is
returned by \code{\link[=job_result]{job_result()}}; for function targets the function's return value.
}
