% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a2a.R
\name{a2a_server}
\alias{a2a_server}
\title{Serve an agent over the A2A protocol}
\usage{
a2a_server(
  agent,
  host = "127.0.0.1",
  card = NULL,
  name = NULL,
  description = NULL,
  skills = list(),
  timeout = 30
)
}
\arguments{
\item{agent}{An agent from \code{\link[=chat_agent]{chat_agent()}}, \code{\link[=react_agent]{react_agent()}}, etc.}

\item{host}{Interface to bind (default "127.0.0.1")}

\item{card}{Optional \code{\link[=agent_card]{agent_card()}}; built automatically when omitted}

\item{name}{Agent name for the auto-built card (defaults to the agent's
description, else "agent")}

\item{description}{Agent description for the auto-built card}

\item{skills}{Skills for the auto-built card}

\item{timeout}{Seconds to wait for the server to report readiness}
}
\value{
An A2A server handle (class \code{agentgraph_a2a_server})
}
\description{
Starts a background server (a separate R process) that exposes \code{agent} via
a minimal A2A protocol: \code{GET /.well-known/agent.json} returns the agent
card and \verb{POST /} handles JSON-RPC 2.0 \code{tasks/send} and \code{message/send}.
Returns a handle; stop it with \code{\link[=a2a_stop]{a2a_stop()}}.
}
\details{
Only graph-based agents (chat, react, plan-execute, reflection) can be
served — they serialize cleanly to the server process. A \code{router_agent()}
carries an R closure that cannot be serialized, so it is rejected.
}
