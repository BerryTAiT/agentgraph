% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{llama_server}
\alias{llama_server}
\title{Launch a local llama.cpp server and return a ready provider}
\usage{
llama_server(
  model_path,
  binary = Sys.getenv("LLAMA_SERVER", "llama-server"),
  host = "127.0.0.1",
  port = 8080L,
  n_ctx = NULL,
  n_gpu_layers = NULL,
  extra_args = character(),
  timeout = 60
)
}
\arguments{
\item{model_path}{Path to a \code{.gguf} model file}

\item{binary}{Path to the \code{llama-server} executable; defaults to
\code{llama-server} on \code{PATH}}

\item{host}{Interface to bind (default \verb{127.0.0.1})}

\item{port}{TCP port to bind (default \code{8080})}

\item{n_ctx}{Context window size (optional; passed through if given)}

\item{n_gpu_layers}{GPU layers to offload; \code{NULL} leaves llama.cpp defaults}

\item{extra_args}{Additional command-line arguments passed verbatim}

\item{timeout}{Seconds to wait for the server to start listening}
}
\value{
A list with \code{provider}, \code{process}, \code{host}, \code{port}, \code{model_path}
}
\description{
Starts llama.cpp's \code{llama-server} binary against a GGUF model, waits until it
reports it is listening, and returns a handle containing a ready \code{provider}
(for \code{chat()} / \code{run()} / graph nodes) plus the underlying process so you can
stop it later with \code{llama_server_stop()}.
}
