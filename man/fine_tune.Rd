% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finetune.R
\name{fine_tune}
\alias{fine_tune}
\title{Create an OpenAI-compatible fine-tuning job}
\usage{
fine_tune(
  provider,
  examples,
  model = "gpt-4o-mini",
  suffix = NULL,
  n_epochs = NULL
)
}
\arguments{
\item{provider}{A provider configuration (its \code{base_url} + \code{api_key} are used)}

\item{examples}{A data.frame (with \code{input} + \code{output}/\code{expected}/\code{correction})
or a list of \code{list(input, output)} pairs}

\item{model}{Base model to fine-tune}

\item{suffix}{Optional job suffix}

\item{n_epochs}{Optional number of epochs}
}
\value{
The fine-tuning job ID (a string)
}
\description{
Converts \code{examples} to a JSONL chat training set, uploads it, and creates a
fine-tuning job on \code{model}. Returns the job ID; poll \code{\link[=fine_tune_status]{fine_tune_status()}}
until \code{status == "succeeded"} and use its \code{fine_tuned_model} as the model in
a provider.
}
