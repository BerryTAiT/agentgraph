% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{resume}
\alias{resume}
\title{Resume a graph that paused at an interrupt node}
\usage{
resume(
  graph,
  interrupted_state,
  tools = list(),
  inject = list(),
  n_threads = 0L,
  on_token = NULL
)
}
\arguments{
\item{graph}{The same graph object passed to run()}

\item{interrupted_state}{The state list returned by the interrupted run()}

\item{tools}{List of tool definitions}

\item{inject}{Named list of state values to set/override before resuming
(e.g. \code{list(approved = TRUE)}); values are JSON-encoded automatically}

\item{n_threads}{Thread pool size for parallel nodes (0 = auto)}

\item{on_token}{Optional callback invoked for each streamed token}
}
\value{
The final state after resuming (may itself be interrupted again)
}
\description{
Takes the state returned by a previous interrupted run() (or resume()) and
continues execution from the interrupt node's outgoing edge. The full state
(messages + data) is round-tripped back into the C++ engine, so any values
set before the pause are preserved.
}
