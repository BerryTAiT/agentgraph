% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy.R
\name{file_tools_policy}
\alias{file_tools_policy}
\title{Sandbox the native built-in file tools}
\usage{
file_tools_policy(allow = character(), deny = character())
}
\arguments{
\item{allow}{Character vector of directories file tools may access
(recursively). Empty means no allow-list.}

\item{deny}{Character vector of directories file tools must never access.}
}
\value{
Invisibly \code{TRUE}
}
\description{
Sets the process-level filesystem policy enforced by the C++ engine for the
native \code{read_file} and \code{write_file} tools. Because those tools execute
in-process, the R-side \code{\link[=restrict_tool]{restrict_tool()}} wrapper cannot intercept them; this
helper is the equivalent for the native tools. When \code{allow} is non-empty,
every path the LLM passes to a file tool must resolve under one of those
directories; paths under \code{deny} are always rejected. Paths are canonicalized
in C++ (resolving \code{..} and symlinks) before the check, so directory-traversal
escapes are blocked.
}
\details{
The policy is process-wide and applies from the first time the native tools
are used after the environment is set (call it at session start, before any
\code{\link[=run]{run()}}). Pass empty vectors to both arguments to clear the policy (only
effective if called before the first file-tool use in the process).
}
\examples{
\dontrun{
# Allow file tools only under the project data directory
file_tools_policy(allow = file.path(getwd(), "data"))
}
}