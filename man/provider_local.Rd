% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local.R
\name{provider_local}
\alias{provider_local}
\title{Create a local OpenAI-compatible provider configuration}
\usage{
provider_local(
  model = "local-model",
  base_url = "http://127.0.0.1:8080/v1",
  max_tokens = 4096,
  temperature = 0.7,
  max_retries = 3L,
  retry_base_delay_ms = 500L,
  retry_max_delay_ms = 8000L,
  requests_per_minute = 0L
)
}
\arguments{
\item{model}{Model name as served by the local server (e.g. "mistral-7b")}

\item{base_url}{Base URL of the local server}

\item{max_tokens}{Maximum tokens in response}

\item{temperature}{Sampling temperature}

\item{max_retries}{Retry transient HTTP failures (429/5xx); 0 disables}

\item{retry_base_delay_ms}{Initial backoff delay (ms), doubles each attempt}

\item{retry_max_delay_ms}{Upper bound on the backoff delay (ms)}

\item{requests_per_minute}{Token-bucket request cap; 0 disables rate limiting}
}
\value{
A provider configuration list
}
\description{
Points the engine at any local server that speaks the OpenAI chat-completions
protocol: llama.cpp's \code{llama-server}, LM Studio, vLLM, Ollama, etc. Runs fully
offline with no API key.
}
