% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{provider_azure}
\alias{provider_azure}
\title{Create an Azure OpenAI provider configuration}
\usage{
provider_azure(
  api_key = Sys.getenv("AZURE_OPENAI_API_KEY"),
  deployment,
  endpoint = Sys.getenv("AZURE_OPENAI_ENDPOINT"),
  api_version = "2024-10-21",
  max_tokens = 4096,
  temperature = 0.7,
  max_retries = 3L,
  retry_base_delay_ms = 500L,
  retry_max_delay_ms = 8000L,
  requests_per_minute = 0L
)
}
\arguments{
\item{api_key}{Azure OpenAI key (or set AZURE_OPENAI_API_KEY env var)}

\item{deployment}{Your Azure deployment name (required)}

\item{endpoint}{Resource endpoint, e.g. "https://my-resource.openai.azure.com"
(or set AZURE_OPENAI_ENDPOINT env var)}

\item{api_version}{Azure API version string}

\item{max_tokens}{Maximum tokens in response}

\item{temperature}{Sampling temperature}

\item{max_retries}{Retry transient HTTP failures (429/5xx); 0 disables}

\item{retry_base_delay_ms}{Initial backoff delay (ms), doubles each attempt}

\item{retry_max_delay_ms}{Upper bound on the backoff delay (ms)}

\item{requests_per_minute}{Token-bucket request cap; 0 disables rate limiting}
}
\value{
A provider configuration list
}
\description{
Azure uses the \code{api-key} header (not Bearer) and requires a deployment
name plus an API version in the URL; both are handled by the C++ engine
when the provider name is "azure".
}
