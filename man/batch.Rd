% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{batch}
\alias{batch}
\title{Run a target over many inputs}
\usage{
batch(target, inputs, ..., concurrency = 1L, on_result = NULL)
}
\arguments{
\item{target}{An agent, a graph, or a function}

\item{inputs}{A character vector or list of inputs (length >= 1)}

\item{...}{Extra arguments forwarded to the target on every input}

\item{concurrency}{Number of parallel workers (1 = sequential)}

\item{on_result}{Optional callback \verb{function(row)} invoked with each
result row (\code{list(i, input, answer, error, elapsed)}); in parallel mode
rows are reported in input order once all are complete}
}
\value{
A data frame of results (class \code{agentgraph_batch})
}
\description{
Executes \code{target} once per input and returns a data frame with one row per
input: \code{i}, \code{input}, \code{answer}, \code{error}, and \code{elapsed} (seconds). Errors on
individual inputs are contained — the row records the message and the run
continues.
}
\details{
\code{target} may be:
\itemize{
\item an agent (from \code{\link[=chat_agent]{chat_agent()}}, \code{\link[=react_agent]{react_agent()}}, ...): each input must be
a single string, passed to \code{\link[=run_agent]{run_agent()}};
\item a graph (from \code{\link[=state_graph]{state_graph()}}): each input is either a single string
(wrapped as a user message) or a full state list, passed to \code{\link[=run]{run()}};
\item a function: called as \code{target(input, ...)}; its return value is used as
the answer (a list with an \code{answer} element is unwrapped).
}

With \code{concurrency = 1} (default) inputs are processed sequentially in the
current process. With \code{concurrency > 1} inputs are distributed over that
many mirai daemons (separate R processes; requires the mirai package).
Existing daemons you have already set are reused and left running;
otherwise batch() starts its own and tears them down afterwards. In
parallel mode the target is serialized to the daemons, so a function
target must be self-contained (reference only its arguments and installed
packages, not objects from your global environment).
}
