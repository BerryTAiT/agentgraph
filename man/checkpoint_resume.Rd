% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{checkpoint_resume}
\alias{checkpoint_resume}
\title{Resume a graph from a crash-durable checkpoint}
\usage{
checkpoint_resume(
  graph,
  checkpoint_path,
  tools = list(),
  inject = list(),
  n_threads = 0L,
  on_token = NULL,
  on_event = NULL,
  log_path = NULL,
  max_total_tokens = 0L,
  max_time_sec = 0,
  max_cost_usd = 0
)
}
\arguments{
\item{graph}{The same graph object passed to the original run()}

\item{checkpoint_path}{Path to a checkpoint file written by the engine}

\item{tools}{List of tool definitions}

\item{inject}{Named list of state values to set/override before resuming}

\item{n_threads}{Thread pool size for parallel nodes (0 = auto)}

\item{on_token}{Optional callback invoked for each streamed token}

\item{on_event}{Optional callback invoked for engine lifecycle events}

\item{log_path}{Optional file path to append a JSONL trace to while resuming}

\item{max_total_tokens}{Kill switch on cumulative token usage (0 = unlimited)}

\item{max_time_sec}{Kill switch on wall-clock seconds (0 = unlimited)}

\item{max_cost_usd}{Kill switch on estimated USD cost (0 = unlimited)}
}
\value{
The final state after resuming
}
\description{
Loads a checkpoint file, then continues execution from the node where the
previous run stopped. If the checkpoint marks the run as already complete
(\code{resume_node} empty or \code{"__end__"}), the saved state is returned
unchanged. When \code{checkpoint_path} is reused, execution keeps appending
checkpoints so the run remains crash-safe across resumptions.
}
