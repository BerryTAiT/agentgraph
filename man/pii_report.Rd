% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit.R
\name{pii_report}
\alias{pii_report}
\title{Summarize the PII that \code{\link[=pii_scrub]{pii_scrub()}} would redact}
\usage{
pii_report(
  text,
  entities = c("email", "api_key", "ssn", "credit_card", "phone", "ipv4")
)
}
\arguments{
\item{text}{A character vector of strings to scan}

\item{entities}{Entity types to count (any subset of the six PII types)}
}
\value{
A data.frame with columns \code{entity} and \code{count}
}
\description{
Counts, per entity type, the occurrences in \code{text} that \code{\link[=pii_scrub]{pii_scrub()}} would
replace. Use the result (or a subset) as the \code{pii} argument to
\code{\link[=audit_record]{audit_record()}} to build a "what was redacted" audit trail.
}
