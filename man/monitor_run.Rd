% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monitor.R
\name{monitor_run}
\alias{monitor_run}
\title{Run a graph with a live console monitor}
\usage{
monitor_run(
  graph,
  state,
  tools = list(),
  state_data = list(),
  n_threads = 0L,
  live = interactive(),
  color = live
)
}
\arguments{
\item{graph}{A graph object (from \code{state_graph()})}

\item{state}{Initial state as a list of messages}

\item{tools}{List of tool definitions}

\item{state_data}{Named list of additional state data}

\item{n_threads}{Thread pool size for parallel nodes (0 = auto)}

\item{live}{When TRUE (the default in interactive sessions), paint a live
ANSI-updating panel. Set FALSE for a plain one-line-per-event log.}

\item{color}{When TRUE, colourise the panel with ANSI codes.}
}
\value{
The final state after graph execution (same as \code{run()})
}
\description{
Behaves exactly like \code{\link{run}} but renders a live status panel while
the graph executes, showing LLM API connection status, thread-pool size,
current node/iteration, per-node timings, and a rolling event log.
}
