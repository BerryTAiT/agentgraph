% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voice.R
\name{voice_run}
\alias{voice_run}
\title{Run a graph through a voice loop (STT -> agent -> TTS)}
\usage{
voice_run(
  graph,
  state = NULL,
  text = NULL,
  audio_file = NULL,
  stt_provider = NULL,
  tts_provider = NULL,
  output_file = NULL,
  ...
)
}
\arguments{
\item{graph}{A graph (from \code{\link[=state_graph]{state_graph()}})}

\item{state}{Initial state (or NULL to use \code{text})}

\item{text}{Input text (skips STT when given)}

\item{audio_file}{Optional input audio (transcribed when given)}

\item{stt_provider}{An STT provider (required when \code{audio_file} is given)}

\item{tts_provider}{Optional TTS provider (skips TTS when NULL)}

\item{output_file}{Optional audio output path}

\item{...}{Extra arguments forwarded to \code{\link[=run]{run()}}}
}
\value{
A list with \code{text}, \code{answer}, \code{audio}, and \code{state}
}
\description{
Transcribes \code{audio_file} (when given) to text, runs \code{graph} on that text,
and synthesizes the final answer back to audio. Returns the intermediate
text, the answer, the audio file path, and the graph state.
}
