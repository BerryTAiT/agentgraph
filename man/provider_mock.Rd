% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock.R
\name{provider_mock}
\alias{provider_mock}
\title{Create an offline mock LLM provider}
\usage{
provider_mock(responses = list(`*` = "I don't know"), model = "mock")
}
\arguments{
\item{responses}{A named or unnamed list of responses}

\item{model}{Model name reported by the mock}
}
\value{
A provider configuration list
}
\description{
Returns an OpenAI-compatible provider backed by a built-in mock server (no
Python, no API key, no network). Responses are scripted:
}
\details{
\itemize{
\item A named list maps input text -> response: each request's last user message
is matched exactly against the names, falling back to a \code{"*"} entry, then
to the first entry. E.g. \code{list("What is 2+2?" = "4", "*" = "I don't know")}.
\item An unnamed list (or character vector) serves responses in order, repeating
the last one.
}

Each response may be a string (the assistant text) or a list with \code{content},
\code{finish_reason}, \code{tool_calls} (\code{list(id, name, arguments)}), \code{usage}, \code{model}.
Stop all mock servers with \code{\link[=mock_stop_all]{mock_stop_all()}}.
}
