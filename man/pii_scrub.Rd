% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pii.R
\name{pii_scrub}
\alias{pii_scrub}
\title{Redact common PII from text}
\usage{
pii_scrub(
  text,
  redact = "[REDACTED]",
  entities = c("email", "api_key", "ssn", "credit_card", "phone", "ipv4")
)
}
\arguments{
\item{text}{A character vector of strings to scrub}

\item{redact}{Replacement text (default "[REDACTED]")}

\item{entities}{Character vector naming which entity types to scrub; any
subset of \code{"email"}, \code{"api_key"}, \code{"ssn"},
\code{"credit_card"}, \code{"phone"}, \code{"ipv4"}}
}
\value{
A character vector of the same length as \code{text}
}
\description{
Replaces email addresses, API keys, US Social Security numbers, credit-card
numbers, US phone numbers, and IPv4 addresses with \code{redact}. The default
entity set covers all six; pass \code{entities} to restrict which are scrubbed.
Processing always follows a canonical order regardless of the order in
\code{entities}, so a 16-digit card number is redacted as one unit rather than
being partially consumed by the phone-number pattern.
}
